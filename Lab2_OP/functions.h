using namespace  std;
#include "struct.h"

Breaks getData(Breaks);
bool check(Breaks, Breaks, int, string);
void displayData(Breaks);
void createFile(string);
void readFile(string);
int enterNum(string);
void fullCheckUp(string);